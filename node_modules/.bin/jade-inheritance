#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../jade-inheritance/bin/jade-inheritance" "$@"
  ret=$?
else 
  node  "$basedir/../jade-inheritance/bin/jade-inheritance" "$@"
  ret=$?
fi
exit $ret
